<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dell.designapp">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="multiplexer.lab.budget.catcher.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="multiplexer.lab.budget.catcher.SignInOne" />
        <activity android:name="multiplexer.lab.budget.catcher.SignInTwo" />
        <activity android:name="multiplexer.lab.budget.catcher.SignUp" />
        <activity android:name="multiplexer.lab.budget.catcher.AddProfilePic" />
        <activity android:name="multiplexer.lab.budget.catcher.AccountCreated" />
        <activity android:name="multiplexer.lab.budget.catcher.Recycler" />
        <activity android:name="multiplexer.lab.budget.catcher.RecyclerViewAdapter" />
        <activity android:name="multiplexer.lab.budget.catcher.AddPaymentMethod" />
        <activity android:name="multiplexer.lab.budget.catcher.RecyclerViewAdapterTwo" />
        <activity android:name="multiplexer.lab.budget.catcher.InputCardInfo" />
        <activity android:name="multiplexer.lab.budget.catcher.ConnectFinances" />
        <activity android:name="multiplexer.lab.budget.catcher.AutoSave" />
        <activity android:name="multiplexer.lab.budget.catcher.CategoryTransactions" />
        <activity android:name="multiplexer.lab.budget.catcher.NewBillSelectRecurringTransaction" />
        <activity android:name="multiplexer.lab.budget.catcher.ABetterCard" />
        <activity android:name="multiplexer.lab.budget.catcher.Overview" />
        <activity android:name="multiplexer.lab.budget.catcher.fragment.ExpensesFragment" />
        <activity android:name="multiplexer.lab.budget.catcher.fragment.BillsFragment" />
        <activity android:name="multiplexer.lab.budget.catcher.fragment.AllowancesFragment" />
        <activity android:name="multiplexer.lab.budget.catcher.NewBillFinish" />
        <activity android:name="multiplexer.lab.budget.catcher.AddNewIncidental" />
        <activity android:name="multiplexer.lab.budget.catcher.Expenses" />
        <activity android:name="multiplexer.lab.budget.catcher.InputAccountInfo" />
        <activity android:name="multiplexer.lab.budget.catcher.RecyclerViewAdapterThree" />
        <activity android:name="multiplexer.lab.budget.catcher.OverviewSetWeeklyBudget" />
        <activity android:name="multiplexer.lab.budget.catcher.AllowancesSetBudget" />
        <activity android:name="multiplexer.lab.budget.catcher.fragment.HomeFragment" />
        <activity android:name="multiplexer.lab.budget.catcher.fragment.ManageFragment" />
        <activity android:name="multiplexer.lab.budget.catcher.fragment.AccountFragment" />
        <activity android:name="multiplexer.lab.budget.catcher.fragment.AnalysisFragment" />
        <activity android:name="multiplexer.lab.budget.catcher.BottomNavigationn" />
        <activity android:name="multiplexer.lab.budget.catcher.Graph" />
        <activity android:name="multiplexer.lab.budget.catcher.HomeActivity"></activity>
    </application>

</manifest>